{
  "name": "astro-tools",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/preact": "^4.1.1",
    "astro": "^5.14.5",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
